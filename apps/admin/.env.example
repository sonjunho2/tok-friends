# API Base URL
NEXT_PUBLIC_TOK_API_BASE=https://tok-friends-api.onrender.com

# Admin JWT (API가 전역 JWT 가드 사용 중이므로, 임시로 토큰을 넣어 호출)
# 실제 운영에서는 Admin 로그인 화면을 만들어 발급받은 토큰을 여기에 주입하거나
# 서버 사이드에서 안전하게 주입하도록 전환합니다.
NEXT_PUBLIC_TOK_ADMIN_JWT=
